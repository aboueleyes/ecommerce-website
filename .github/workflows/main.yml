name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{bef42c08-2b60-48da-a99c-2472d0df4637}}
          heroku_app_name: "we-are-not-200" #Must be unique in Heroku
          heroku_email: "ibrahimabouelanin@gmail.com"
